<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Random_Quote_Generator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <!-- Light Theme -->
    <style name="Theme.Random_Quote_Generator.Light" parent="Base.Theme.Random_Quote_Generator">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Dark Theme -->
    <style name="Theme.Random_Quote_Generator.Dark" parent="Base.Theme.Random_Quote_Generator">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
